{
  "name": "ninety-app",
  "version": "1.0.0",
  "description": "Team operating system — Rocks & Issues tracker",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --env-file=.env server.js",
    "test": "node --test --test-reporter=spec",
    "test:pg": "node --test --test-reporter=spec --test-concurrency=1 test/postgres/smoke.test.js"
  },
  "dependencies": {
    "connect-pg-simple": "^10.0.0",
    "express": "^4.18.2",
    "express-session": "^1.19.0",
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "supertest": "^7.0.0"
  }
}
